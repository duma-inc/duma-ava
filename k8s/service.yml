apiVersion: v1
kind: Service
metadata:
  name: duma-ava
  namespace: duma
  labels:
    app: duma-ava
spec:
  type: NodePort
  selector:
    app: duma-ava
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30317
